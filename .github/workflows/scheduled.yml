name: Scheduled Tasks

on:
  schedule:
    - cron: '0 3 * * *'

jobs:
  nightly-job:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run tasks
      run: echo "Running scheduled tasks"
